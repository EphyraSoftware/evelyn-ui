import {PopoutHostDirective} from './popout-host.directive';

describe('PopoutHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoutHostDirective();
    expect(directive).toBeTruthy();
  });
});
